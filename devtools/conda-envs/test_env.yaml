name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - numba
  - scipy
  - matplotlib
  - statsmodels

    # Testing
  - pytest
  - pytest-cov
  - pytest-env
  - codecov
